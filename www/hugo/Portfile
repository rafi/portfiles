# -*- coding: utf-8; mode: tcl; tab-width: 4; indent-tabs-mode: nil; c-basic-offset: 4 -*- vim:fenc=utf-8:ft=tcl:et:sw=4:ts=4:sts=4

PortSystem          1.0
PortGroup           github 1.0

github.setup        spf13 hugo 0.26 v
github.tarball_from releases

categories          www
platforms           darwin
license             MIT

maintainers         {gmail.com:justrafi @rafi} openmaintainer
description         Fast and Flexible Static Site Generator built with love
long_description    ${name}: ${description}

checksums           ${name}_${version}_macOS-64bit.tar.gz \
                    rmd160  17562237a5182387987f7f0e753004291dfa67bc \
                    sha256  a6b409aafaee05cc4b70ce054a7e1e45dd50271069c24af9047f99c7d40d08bc \
                    ${name}_${version}_macOS-32bit.tar.gz \
                    rmd160  57623cb8778cc9b441ed5711f1362bb22c3a4545 \
                    sha256  86a40d3fe6bc17784df160b139c528415a26b559187a83a62a34719e8b527a61

supported_archs     x86_64 i386
use_configure       no

depends_run-append  path:etc/bash_completion:bash-completion

distname            ${name}_${version}
extract.mkdir       yes
if {$build_arch == "x86_64"} {
    extract.suffix _macOS-64bit.tar.gz
} else {
    extract.suffix _macOS-32bit.tar.gz
}

build    {}
destroot {
    xinstall -m 0755 ${worksrcpath}/${name} \
        ${destroot}${prefix}/bin/${name}

    # Man pages
    system -W ${destroot}${prefix}/bin \
        "./hugo gen man --dir=${destroot}${prefix}/share/man/man1"

    # Bash autocompletion
    xinstall -d -m 0755 ${destroot}${prefix}/etc/bash_completion.d
    system -W ${destroot}${prefix}/bin \
        "./hugo gen autocomplete --completionfile=${destroot}${prefix}/etc/bash_completion.d/hugo"
}
